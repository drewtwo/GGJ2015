var task = {
    // Application Constructor
    initialize: function() {
		$("div#task_field").text("Find and touch a nearby brick with one finger, is anyone else doing the same?");
	}
};

task.initialize();